language: node_js
node_js: "8"

notifications:
  slack: $SLACK_NOTIFICATION

script:
- echo "npm test temporarily disabled"
- npm run test:ci
- npm run build

before_deploy:
  - cd dist/kalidea/kaligraphi

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  skip_cleanup: true
  on:
    tags: true
