import { KalDropdownDirective } from './kal-dropdown.directive';

describe('KalDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new KalDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
