import { KalTabLabelDirective } from './kal-tab-label.directive';

describe('KalTabLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new KalTabLabelDirective();
    expect(directive).toBeTruthy();
  });
});
