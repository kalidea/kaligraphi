import { KalListItemDirective } from './kal-list-item.directive';

describe('KalListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KalListItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
