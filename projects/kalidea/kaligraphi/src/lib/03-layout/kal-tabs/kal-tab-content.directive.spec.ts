import { KalTabContentDirective } from './kal-tab-content.directive';

describe('KalTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new KalTabContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
