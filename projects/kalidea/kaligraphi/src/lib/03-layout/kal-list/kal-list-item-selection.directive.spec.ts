import { KalListItemSelectionDirective } from 'projects/kalidea/kaligraphi/src/lib/03-layout/kal-list/kal-list-item-selection.directive';

describe('KalListItemSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new KalListItemSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
