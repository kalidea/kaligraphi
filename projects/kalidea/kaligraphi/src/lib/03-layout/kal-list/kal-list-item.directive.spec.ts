import { KalListItemDirective } from 'projects/kalidea/kaligraphi/src/lib/03-layout/kal-list/kal-list-item.directive';

describe('KalListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KalListItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
